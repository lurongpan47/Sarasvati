# 2026-09-22 · 08:30 PDT · Daily push — option 3 (blockchain integrity verification)

**Trigger:** `sarasvati-daily-push` cron. Tree clean, in sync with origin at `e846340`; `.ots_confirmed` absent (stamp pending).

**Decided:** verification day, no content. Ran `scripts/ots_upgrade_watch.sh` → the 09-21 14:25 stamp over `e846340` had confirmed: **968053 / 968054 / 968085** (alice / bob / catallaxy; eternitywall still pending). Script committed and pushed `8c6e44f`. Then:

1. Re-derived the manifest recipe (`git ls-files` minus the three rolling manifest files, `./`-prefixed, sha256, C-sorted) and diffed — byte-identical to the committed `manifests/SHA256SUMS`. `shasum -c` OK on all 238 entries.
2. Checked all nine AWS mirrors: `manifests/SHA256SUMS` sha256 `9ae44e54…` on every host, same as local. The 09-21 seasia commit had been synced already.
3. Recorded the heights in CHANGELOG (new 09-22 section), regenerated the manifest (CHANGELOG + this file are the only changed/new entries), re-stamped to the four calendars, kept the confirmed proof as `SHA256SUMS.pre-2026-09-22.ots.bak`, cleared `.ots_confirmed`, rsynced the nine mirrors.

**Why option 3 and not 1 (REXIII Kālsī audit):** the pending proof was the one thing with a deadline-like quality — leaving a confirmed-but-uncommitted proof risks it being overwritten by the next re-stamp (the 09-21 13:50 entry shows exactly that hazard). REXIII Kālsī (Hultzsch pp. 43–49, page images needed, OCR unusable) is next in the standing audit queue and needs a full session with the scan open, not a daily-push slot. It stays first in line.

**Principles:** sati (heights logged where a reader of the CHANGELOG will find them, not only in `ots info`); satya (mirror sync asserted only after hashing each host; eternitywall marked pending rather than "all four confirmed"); anicca (rolling stamp continues; old proof preserved).

**Rejected:** editing nothing and leaving the heights only in the git commit message of `8c6e44f` — the CHANGELOG is the human-facing audit trail, git log is not.

**Would have blocked me:** `shasum -c` failing on any entry, or a mirror with a divergent manifest hash — either would have turned this into an investigation, not a record.

**Cost:** 1 cycle, ~20k tokens, $0.

---

# 2026-09-22 · 09:06 PDT · Retroactive scan audit #4 — REXIII Kālsī (subagent `sarasvati-rexiii-kalsi-audit`)

**Trigger:** first item of the standing audit queue after the 08:30 verification day; page images n181–n188 of IA `InscriptionsOfAsoka.NewEditionByE.Hultzsch` already downloaded to `.scratch/hultzsch/` at full resolution.

**Decided:** re-read `translations/ashoka-edicts/major-rock-edict-XIII-4lang.md` against the leaves and replace both source columns with Hultzsch's printed text. Established first (satya on the layout before satya on the letters) that Hultzsch does print Roman rows for Kālsī XIII — p. 45 (east face ll. 35–39, A–K, 29 notes) and p. 46 (south face ll. 1–18, O–AA, 23 notes) — so the column is labelled "Hultzsch 1925 Roman transliteration", not a Sarasvatī reading of the Devanāgarī. Found: page references wrong (no plate on p. 45; Roman pp. 45–46, translation pp. 47–49); Prakrit a composite in a foreign convention with emendations as text, brackets closed, and four stretches of words on no rock (`mukhya-mute`, `hida cha`, `shaveshu manuśyeshu`, `chalambu`); English near-verbatim but with the (P) lacuna deleted and a non-Hultzsch gloss inserted; section letters correct; recension note wrong ("fullest complete", "supplied from Shāhbāzgaṛhī or Girnar in brackets"). Corrected all of it with an erratum table; changed the Chinese/Tibetan drafts only where they rendered the invented `mukhya-mute`. CHANGELOG + ROADMAP § 1 updated, queue score 4 of 4.

**Principles:** satya (every letter in the new column was read from a crop of the page; where two reads disagreed — `maṭe`/`mate`, `[ya]`/`[va]`, `pāśaṁḍa`/`pāshaṁḍa`, `ladhesha`/`ladheshu`, `manatu`/`mānatu`, `shamanā`/`shamana` — a tight crop was compared with neighbouring plain letters on the same line before accepting, and the hard cases are listed for the epigraphist rather than presented as settled); sati (the erratum table records was → is for every change, so the correction is itself auditable); ahiṁsā toward the drafts (Chinese/Tibetan not rewritten, only the clause that tracked a non-existent reading); anicca (the 09-15 text stays provably dated by the superseded stamps).

**Rejected:** (a) reading the Devanāgarī block and transliterating it myself — unnecessary once the Roman rows were found, and it would have substituted my convention for his; (b) filling the (P)/(Q) lacunae from Shāhbāzgaṛhī "for readability" — that is exactly what the 09-15 file did and exactly what the erratum corrects; Hultzsch's dots stay; (c) keeping the OCR layer as a source for the Prakrit — it is Devanāgarī garbage there; used for nothing.

**Would have blocked me:** an unreadable leaf (none was — the 1925 Clarendon type at ~5900 px wide resolves the underdots), or Hultzsch printing Kālsī XIII in Devanāgarī only (he does not); either would have left the column marked ⟨བརྟག⟩ draft with the reason stated.

**Cost:** 1 subagent session, ~60 `image` calls over ~110 crops, $0 beyond tokens.
